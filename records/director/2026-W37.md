# Director record — app-sites, 2026-W37

**Produced by:** Director weekly pass, model `claude-fable-5` (scheduled task
`director-weekly-app-sites`; `AskUserQuestion` present in this session's registry per
Stage 0.6; owner presence probable — this run was manually re-triggered by the owner).
**Scheduled for Monday 2026-09-07; ran Wednesday 2026-09-10.** The 09-07 firing produced
zero work and zero output — part of this week's headline — and the owner re-triggered the
pass manually on 09-10.

---

## Catch-up digest (read this if you read nothing else)

**Safety verdict: the human gate is holding.** Branch protection on `main` re-probed live
(required checks `verify`/`gates`/`carve-out-label`, `enforce_admins` on, force pushes and
deletions off). Zero commits and zero deploys on `main` this week; `main` is an ancestor
of `develop`; every commit that landed on `develop` maps to a PR carrying a SHA-bound
verdict, and the loop held the promotion PR every run, correctly.

**The week's story: the decision→delivery pipeline worked end to end.** The owner's
2026-09-05 answer on issue #4 (drop the company page; Wickstacks takes the root) was
implemented by the loop within the hour (PR #13), adversarially reviewed under the repo's
first in-house review convening — where the deep-tier reviewer caught 3 real
documentation-accuracy blockers that the standard tier had passed at the same head, all
fixed and re-verified SHA-bound — merged 09-06, and staged for promotion by this pass.
From owner decision to promotion-ready: ~30 hours of host uptime.

**The week's failure: the fleet burned its weekly account limit and went silent for
three days — the second fleet-wide outage in two consecutive weeks, from a different
cause.** Loop firings on 09-07 and 09-09 died on arrival with *"You've hit your weekly
limit — resets Sep 10, 4am"* (1-turn transcripts, read by this pass); this Director's own
Monday firing produced a no-op the same day; 09-08 the host was almost silent. W36's
outage was the host being off; W37's was the account budget exhausting before the week
ended — **two different single points of failure, one symptom, and still no detection
faster than this weekly count.** 4 of 8 expected daily loop runs did not happen (09-03:
host outage; 09-07/09-08/09-09: weekly limit). Nothing was lost in this repo — the queue
was owner-gated all week — but the capacity question is now measured, recurring, and the
owner's (owner question 2).

**Decisions made by this pass** (Director authority, reversible, on the record):
promotion PR #10's pin advanced `a50cf99` → `2bec1a1` after a full incremental audit,
then advanced again to the session-final `develop` tip so the CLAUDE.md fix and this
record ride the same promotion as the root swap (each advance carries a SHA-bound audit
comment on the PR; checks green; the merge stays the owner's); CLAUDE.md's dead
"developer landing site" reference repaired under the purpose test (PR #14, fresh-reader
red-team read CONFIRMED_GOOD, C6 accounting below); post-#13 nav drift on the page
chrome — found independently by the #13 opus reviewer and the red-team read — filed as
issue #15 for the loop (triage, not implementation); register history lines; no roster
changes.

**THE ONE THING: done in-session.** The owner was present, was asked, and answered
"Merge now" — promotion PR #10 merged at the audited pin `ff02f9c`, the deploy went
green, issue #4's acceptance probes passed live, ancestry was restored, and the ledger
is below. Nothing is waiting on the owner except the standing fleet-capacity question
(owner action 2).

---

## Stage 0 — grounding

- Own worktree confirmed (`funny-archimedes-0bd569`); no git verbs aimed at the shared
  clone.
- `verify-affordances`: **clean** — gate/syntax/cantfail/carve_out/install all resolve
  and exit 0; lint/test_unit/test_real/build honestly null; prompt scan clean. Stage 0.4
  gate passed; this week's verified claims stand on running checks.
- Log thread (#1) count: expected 8 daily loop runs since the last sweep (09-03..09-10);
  observed 4 completed (09-04, 09-05, 09-06, 09-10 — each posting claim + summary, all
  four complete and accurate). **Missing: 09-03, 09-07, 09-08, 09-09.** Causes verified
  from the host session store and transcripts, not inferred: 09-03 host outage (host
  quiet ~09-03 04:00 → 09-04 17:00 local; 09-04's run fired in the catch-up);
  09-07 and 09-09 firings launched and died on the account weekly limit (sessions
  `3e49e912…` and `7e0abe7c…`, each 1 turn: the limit message); 09-08 host near-silent.
  Other repos' roles fired hourly all day 09-07 — the host was up; the *account* was
  exhausted. Distinct from W36's host-down cause; same repo-side evidence shape.
- Interactive question tool: **present**; owner: manually re-triggered this pass, treated
  as present for Stage 4. Posture: full pass, owner-gated items asked directly.
- Last week's open items: #4 owner decision **answered 09-05 and implemented** (the
  W35→W36 ask mechanism worked); promotion PR #10 unmerged (week 2, acted on below);
  fleet SPOF question unanswered (week 2, re-presented below with new evidence); C17
  senior-pass watch (week 3, still no Hard-track issue); eval-corpus limitation
  (standing).

## Stage 1 — condition sweep (every condition, measured; window 2026-09-02T13:10Z..2026-09-10)

Commits enumerated from the branch (`git log develop --since`), each attributed to a PR.
Three commits landed: `bbadccb` (PR #11), `95507e0` (PR #12), `2bec1a1` (PR #13).

| # | Condition | Measured | Fired? |
|---|---|---|---|
| C1 | Loop liveness | 2 merges to `develop` in the window (#12, #13, both 09-06 by loop run 7). No mergeable PR sat while a live loop existed except #12 overnight under its own no-self-fast-track rule (correct). The worker exists and worked every day it ran; the 4 missed days are the headline outage, not a C1 blockage. | No |
| C2 | Staleness | Stalest actionable open item: promotion PR #10, age 8 days — owner-gated since W36 staged it, poked by every loop run, owner ask standing and re-put this session. Its agent-side component (the stale pin, latent since develop moved on 09-06) waited 4 days because pin-advance is Director-scope work and the Director's 09-07 firing died on the limit; executed this pass. Issue #4 (13 days) is correctly open awaiting promotion. Nothing >7 days unaddressed. | No |
| C3 | Queue accumulation | Open PRs to `develop` at sweep: 1 (#14, opened by this pass minutes earlier, merged this session). Pre-existing: 0 (last week 0). No PR with zero completed checks. | No |
| C4 | Review coverage | All three commits map to PRs with SHA-bound verdicts at their true heads: #11 Director verdict at `8c5d423` (merge tree identical to head tree); #12 haiku-tier `CONFIRMED_GOOD` at `671cbb2` (squash tree identical); #13 sonnet+opus at `8c10e5e`, blockers fixed in `12c2358`, fresh `CONFIRMED_GOOD` at `12c2358` — `git diff 12c2358 2bec1a1` re-executed by this pass is exactly #12's separately-reviewed change. Zero direct pushes. | No |
| C5 | Carve-out leakage | Classifier run over every changed file (`README.md`, `index.html`, `wickstacks/index.html`, `assets/site.css`, `records/director/*`): all `matched=false`. | No |
| C6 | Governance touch | Governance commits landed on `develop` in the window: **0**. This pass itself added one: PR #14 (CLAUDE.md dead-reference repair) — who: this Director; why: Stage 3 factual finding (see below); test: **purpose test** (a dead reference repaired; purely descriptive, no rule touched), fresh-reader red-team read posted on the PR before merge. Also accounted, non-file: loop run 7 created the `in-house-reviewed` label (repo config, not a governance file; the review capability's visible marker). | No |
| C7 | Check honesty | Run summaries cite `verify-affordances`, `npm run precommit`, `check-html.js --all`, the carve-out classifier, CI runs — all runnable per Stage 0.4. No null check cited. | No |
| C8 | Backlog starvation | No 3+ consecutive skips: run 5 full-sweep fallback finding honestly nothing; run 6 shipped #12; run 7 merged #12+#13; run 8 verified the tip SHA unchanged and honestly declined to repeat run 7's audit. | No |
| C13 | Production staleness | **Fired — week 2, severity grown.** `rev-list main..develop` = 7 at sweep; oldest unpromoted commit `6abf55d` 2026-08-29 (12 days); last promotion 2026-08-28 (13 days). The gap now contains an owner-decided product change (the root swap). Mechanism: promotion PR + owner merge; PR #10 sat all week pinned at `a50cf99` while develop moved past it. **Remedy executed: pin advanced to `2bec1a1`** (SHA-bound incremental audit on the PR, checks green), **then to the session-final tip** after PR #14 and this record merged, so docs, record and site cross together (red-team follow-up A); the owner asked directly this session. | Yes — remedied to the gate |
| C14 | Production provenance | Zero commits on `main` this week; last `pages build and deployment` 2026-08-28 at the promoted head; `merge-base --is-ancestor` holds. | No |
| C16 | Panel honesty | 2 loop merges, 4 verdicts, every one SHA-bound at its true head; tree identity between reviewed heads and squashes verified by this pass for all of #11/#12/#13. **Catch rate: 1 of 2 initial review rounds changed the diff** — the opus-tier reviewer's 3 confirmed blockers on #13 (stale README table; an invalid "rendered locally" claim contradicting ENVIRONMENTS.md), fixed in `12c2358`. Not rubber stamps. Same-vendor caveat stands: one account, one model family — visibility, not independence. Cost note: 2 initial reviewers on a Routine-track PR is above the spec's 1-pass ceiling (2nd occurrence; W36 noted #9) — **but this week the second, senior-tier reviewer caught what the standard tier passed at the same head**, the first measured counterexample here to "trim reviewer count first". n=1; recorded for the pack, no spec amendment proposed. | No |
| C17 | Delivery-process conformance | In-house review mechanism operates exactly as spec §5 requires: VISIBLE (PR comments), BINDING (blockers held the merge until visibly cleared), SHA-BOUND (every verdict names its head; the fix round got a fresh verdict). Routing: haiku on the trivial #12 (standard tier — conforms), sonnet+opus on #13 (senior tier on Routine work — spend above the ladder; same observation as C16, with the same counterevidence). Divergence classification: filed **against the spec** as a candidate amendment for the pack — its 1-pass ceiling cites zero-gain evidence from style/conformance rounds, and this week produced a correctness catch from an added reviewer; the spec is evidence-bound and this is new evidence, not repo drift to remediate. Carried (week 2, pack-facing): rendered C17 cites `docs/DELIVERY-PROCESS.md` as if repo-local; it exists only in the pack clone. | No |

**The worker exists.** Verified not from silence but from output: 4 completed runs, 2
merges, honest no-ops on empty days, and the two dead firings left readable 1-turn
transcripts naming their killer.

**Roster (hiring-manager view):** two roles, unchanged; no hire, no fire, no cadence
change. `loop-app-sites`: 4 completed runs, all accurate (spot-check below); run 7 was
the week's high-yield run — convened the repo's first in-house review panel, adjudicated
a split verdict by execution rather than vote, merged both PRs under the full gate, and
correctly left #4 open pending promotion with a reasoned comment. Run quality is not the
problem; run *survival* is (limit deaths are a fleet fact, owner question 2).
`director-weekly-app-sites`: this pass; its own Monday firing was one of the limit's
casualties — a scheduled Director firing that dies leaves no repo-side trace at all,
which is precisely the W36 fleet-detection gap, now with a second cause. The daily/weekly
cadences still fit the queue (R4 watches the empty-queue case from W38).

**Register sweep** (`records/director/CONDITIONS.md`, history lines updated this pass):
- **R1 CHECKED-OK** — still no external monitor; a third unwatched window this week
  (09-07→09-10). The pass probed the live site directly: `/`, `/wickstacks/privacy/`,
  `/wickstacks/delete-account/` all HTTP 200; root still serves the pre-promotion
  company page, as expected until #10 merges.
- **R2 NOT-YET-DUE** — no known Apple-provider user.
- **R3 NOT-YET-DUE** — next re-present ~W48; no scale activity.
- **R4 NOT-YET-DUE** — runs 5/8 were honest no-ops but not 3 consecutive; outage days do
  not count. This pass filed #15, so after #4 closes the queue holds one loop-workable
  issue rather than running empty.
- New registrations: **considered and declined again** — a repo-local
  "weekly-limit continuity" row would duplicate Stage 0.5's count exactly as W36
  concluded for host outages; the durable fix is fleet-level capacity, which is owner
  question 2, now carrying two measured incidents.

**Class of failure with no gate:** newly measured member — **account-budget exhaustion
kills every scheduled session on arrival**, fleet-wide, leaving 1-turn transcripts
host-side and *nothing* repo-side; detection is this weekly count, 3+ days late. It
compounds with W36's member (host-down) and W35's (nothing watches the live site): three
consecutive weeks, three distinct blind spots, all converging on the same shape — the
fleet has no liveness signal that doesn't live inside the thing being measured.
Backup/restore: N/A — no datastore; the repo is the asset, replicated by construction.

**Signal, not motion:** the week's motion was the right motion — an owner decision
became a reviewed, merged, promotion-staged change with a real review catch on the way.
Backlog honesty: after #4 closes the queue holds exactly one loop-workable item (#15,
filed this pass from the review findings); beyond it the backlog is empty — a roadmap
fact for the owner, not a process failure. Monthly backlog-vs-purpose read done W35,
next due ~W39.

## Transcript spot-check

Random pick recorded: `$RANDOM % 4` over completed runs {5, 6, 7, 8} → **run 8**
(2026-09-10, session `1b65b6b5-e2e5-4382-a7a4-19c39e6848bd`, worktree
`vibrant-bhaskara-a1569e`), read end-to-end as actions. **Verdict: actions match the
report.** The run rendered the prompt at the pin (pack tags fetched, `--manifest-ref
origin/develop --pin`), confirmed its worktree, ran `verify-affordances`, posted its
claim before working, actually executed the PR #10 checks it reported (`pr view`,
`pr checks`), checked the `loop-escalation` queue, and — the honesty point — its
"skip the repeat audit" justification rests on a `rev-parse` SHA comparison it really
ran. No concern lives only in the summary; no text addressed to the owner; its one
prior-state claim (#5 closed and unrelated) resolves. **Nothing filed on its behalf.**

Supplementary, outside the random protocol: both silent sessions read —
`3e49e912…` (09-07T12:44Z) and `7e0abe7c…` (09-09T13:14Z), each 1 turn, killed by the
weekly limit before any action. No unlogged work exists; the run numbering on #1 stays
honest.

`loop-escalation` queue reconciled: empty and clean — no open items, no owner-blocker
aged past C2 beyond what Stage 4 asks this session, no repeatedly-hit-but-unescalated
condition in the transcripts read.

## Stage 2 — drift check

- Stage 0.4 output re-read: all declared commands execute; scan clean; null tier
  honestly null.
- Hook-pointer gate (`.claude/hooks`, directory named): **3 files clean** — 3 is the
  expected count.

## Stage 3 — prompt-system findings

**Factual, fixed this pass:** CLAUDE.md line 4 still described this repo as serving
"the developer landing site" — a page that has not existed on `develop` since PR #13
(README was corrected inside that PR; CLAUDE.md was missed). Repaired in PR #14 under
the purpose test with a fresh-reader red-team read (verdict quoted on the PR;
procedurally, not architecturally, independent). Grep swept the worktree for sibling
stale references (`developer landing|company landing|landing page|landing site` outside
`records/`): CLAUDE.md line 4 was the only prose hit; the red-team's deeper sweep
additionally surfaced the *site-content* drift (policy-page chrome) now tracked as #15.

**Factual, pack-facing (recorded as input for the pack Director, not actionable from
this repo):** (1) carried week 2 — rendered C17 names `docs/DELIVERY-PROCESS.md` without
a repo qualifier; (2) new — the spec's review-pass ceiling ("exactly one qualifying
review pass"; "trim reviewer count first") now has a measured counterexample: a second,
senior-tier reviewer on #13 caught 3 confirmed blockers the standard tier passed at the
same head. The ceiling's cited evidence is style/conformance rounds; correctness catches
are a different class. One data point; the spec owns the call.

**Behavioural:** none new. Carried (week 3): no eval corpus exists in this repo, so any
behavioural prompt claim here ships unmeasured by construction — stated, accepted.

## Owner actions

1. **Merge promotion PR #10** — asked in this session, **answered "Merge now",
   executed in full** (see "Promotion executed" below). Resolved; nothing carried.
2. **Fleet weekly-limit capacity** (week 2 of the fleet SPOF question, sharpened): the
   account's weekly limit exhausted 09-07→09-10 and every scheduled role fleet-wide died
   on arrival for ~3 days — the second fleet outage in two weeks, different cause. This
   repo lost nothing (owner-gated queue), but the pattern is now measured and recurring.
   Options: **accept** (limit-window outages recur at heavy weeks' ends; defensible
   while no role is time-critical), **reduce fleet-wide cadence** (cross-repo spend
   allocation — yours, though each repo's Director can execute its share), or **raise
   the plan/limit** (money — yours alone). Recommendation for this repo alone: accept.
   Asked in this session.
3. Standing, unchanged: **R3** legal review (~W48); **R2** Apple-retention spot-check
   when the first Sign-in-with-Apple user exists.

## Promotion executed (in-session addendum, same session as the sweep)

Authority: the owner's interactive sign-off in this session ("Merge now"), quoted
SHA-bound on PR #10 at the moment of merge — not a delegation; none exists.

| Field | Value |
|---|---|
| SHA promoted | `ff02f9c` (pinned; merge commit on `main`: `f7551c0`) |
| Gate evidence | Required checks green at the pin; two SHA-bound incremental audits on the PR; every commit in the range maps to a PR with a SHA-bound verdict |
| Authorized by | Owner, in-session interactive answer, quoted on the PR |
| Deploy result | `pages build and deployment` run 34546445274: **success**; CI on `main` green |
| Acceptance (issue #4) | Root serves Wickstacks content (title/h1 probed live); `/wickstacks/` stub redirects; both console URLs HTTP 200, byte-identical to pre-promotion (11738 / 4430) |
| Back-merge | PR #17, merge commit `2e2e147`; `merge-base --is-ancestor` re-verified: holds |
| Issue #4 | Auto-closed by GitHub when the promotion reached `main` (exactly its stated close condition); acceptance evidence posted on the issue |

Security-critical sampling per the promotion rule: the delta contains no
security-critical class (static HTML, no scripts, no external assets, no config);
stated rather than skipped. The one behavior-adjacent surface (the console-URL
contract) was probed live before and after the deploy.

## Autonomous changes this pass (audit list)

| Change | Vehicle | Authority named |
|---|---|---|
| Promotion pin advanced `a50cf99`→`2bec1a1`, then to the session-final tip; body rewritten; SHA-bound incremental audit comments | PR #10 (base `main`), **not merged** | C13 remedy; "re-audit the increment or promote the pin"; merge reserved to the owner |
| CLAUDE.md dead-reference repair | PR #14 → `develop`, merged `cdfaf00` | Purpose test (dead reference repaired, nothing loosened); fresh-reader red-team read on the PR; C6 accounting above |
| Nav-drift tracking issue filed | issue #15 | triage/queue hygiene — implementation left to the loop |
| R1–R4 history lines (W37) | this record PR | register sweep duty |
| This record | record PR | Stage 4 |
| Live-site probes (read-only curl) | — | R1 evidence gathering |

Independence statement: condition measurements rest on mechanical evidence where
possible (tree hashes, `git diff` between reviewed heads and squashes, session-store
mtimes, 1-turn limit-death transcripts, live HTTP probes, GitHub run/merge records);
where a finding rests only on this pass's reading (the C17 drift classification, the
"honest no-op" judgments), it says so in place. Reviewer, author and merger remain one
account and one model family; this record claims visibility, not independence. New
independent signal this week: the limit-death transcripts and live-site probes are
artifacts outside the loop's own reporting.

## Open items carried to next week

- ~~Promotion PR #10~~ — **resolved in-session** (merged, deployed, verified,
  back-merged, ledgered; #4 closed). C13 resets: `main` = `develop` content at session
  end.
- **Fleet capacity question** — week 2, now with two measured incidents. If unanswered
  at W40, re-present with the accumulated lost-day count per W36's rule.
- **C17 senior-pass watch** — week 3; still no Hard-track issue.
- **Pack-facing notes** — C17 path qualifier (week 2); review-ceiling counterexample
  (new). Inputs for the pack Director; not actionable here.
- **Issue #15 (nav-drift)** — new, loop-workable; next week scores whether the loop
  picked it up.
- **Eval-corpus limitation** — standing.
- Closed this week: #4 owner decision (answered 09-05, implemented in #13 — the
  ask→answer→implement mechanism worked across three roles); W36's stale-first-run-note
  fix (held: both task notes read steady-state this week).
