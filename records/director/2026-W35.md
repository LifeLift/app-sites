# Director record — app-sites, 2026-W35

**Produced by:** Director weekly pass, 2026-08-29, model `claude-fable-5` (session launched
by scheduled task `director-weekly-app-sites`; owner not interactively present —
`AskUserQuestion` absent from this session's tool registry, probed per Stage 0.6).
**First Director record for this repo.** There is no last-week file; Stage 0 input was the
adoption-gap checklist on pack issue kaiserguy/agent-project-template#165.

---

## Catch-up digest (read this if you read nothing else)

**Safety verdict: the human gate is holding.** Branch protection on `main` is real
(3 required checks, `enforce_admins` on — this pass's own direct push to `develop` was
mechanically refused, which is the protection working); the one promotion (PR #2) was the
owner's own merge; the loop is confined to `develop` and, when it found itself running as
a model the manifest didn't name, it **authored nothing rather than fake an attribution**
— twice. The gates failed safe.

**The week's headline:** the loop was fully wedged — not broken, wedged *correctly*. Both
runs executed as `claude-sonnet-5` against a manifest pinning `claude-fable-5`, so the
attribution gate stopped all commit-producing stages (issue #5). The weekly pass traced
the root cause: the ratified delivery spec (`docs/DELIVERY-PROCESS.md` §3, owner sign-off
2026-08-15) routes execution-tier work to **sonnet-class** — the scheduler conformed, the
manifest pin was the drift. (Provenance, corrected by the red-team: the pin was
authored fresh at adoption, not inherited — the template's example pins opus-class —
and no recorded owner decision sits behind it either way.) Fixed in PR #7 under
the repair test; the loop renders from `origin/develop` at run time, so the 2026-08-30 run
should clear. Verification hook registered (CONDITIONS.md R5).

**Decisions made by this pass** (Director authority, all reversible, all on the record):
manifest model block corrected (PR #7); dead escalation-hotline retired from the manifest
(PR #7); back-merge repair restoring promotion ancestry (PR #6); retroactive promotion
ledger written on PR #2; `needs-senior-model` label created; missed owner-decision inside
issue #4 surfaced durably on the issue.

**THE ONE THING:** answer the product question inside issue #4 — *should the site root
become the Wickstacks page, and if so where does the LifeLift developer landing move?*
It is the only owner-gated part of the only backlog item, and once #5 clears (expected
2026-08-30) it is the only thing between the loop and a dry queue.

---

## Stage 0 — grounding

- Own worktree confirmed (`magical-wozniak-7378e6`); no git verbs aimed at the shared clone.
- `verify-affordances`: **clean** — every declared command resolves; lint/test_unit/
  test_real/build are declared null and no prompt cites them. Stage 0.4 gate passed, so
  the week's "verified" claims stand on running checks.
- Log thread (#1): **2 summaries for 2 days since install** (2026-08-28 install-day run
  posted 03:17Z; 2026-08-29 scheduled run posted 12:48Z). Expected count met — no gap.
  The trail reconstructs fully from summaries + issues #4/#5 + PRs #2/#3.
- Interactive question tool: absent; owner not present. Posture: owner-absent pass —
  everything safe executed, owner items pre-staged below.
- **Adoption completeness** (first-run checklist from pack #165): always-loaded file ✓
  (CLAUDE.md + session-start hook, 3 hook files, pointer-gate clean); labels ✓
  (`loop-escalation`, `needs-prod-review`, `area/site`, `blocked-owner`; this pass added
  the spec's missing `needs-senior-model`); branch protection ✓ on **both** branches
  (required checks `verify`/`gates`/`carve-out-label`, `enforce_admins`, no force pushes);
  log issue ✓ (#1, active). `records/director/` did not exist — this file starts it.
  Pack #165's close rule ("a durable write from an app-sites loop or Director run exists")
  is now satisfied twice over (loop posts on #1; this record). That issue is the pack
  Director's to close on its own rotation — no cross-repo write from this pass.

## Stage 1 — condition sweep (every condition, measured, 7-day window)

| # | Condition | Measured | Fired? |
|---|---|---|---|
| C1 | Loop liveness | 2 merges to `develop` this week (PR #3 owner-content 08-28; PR #6 this pass). No mergeable PR sat unmerged. **But see the worker-exists paragraph below — the loop itself could author nothing.** | No (headline handled as the model-block finding) |
| C2 | Staleness | Stalest actionable open item: issue #4, **age 1 day**. Nothing near 7 days. | No |
| C3 | Queue accumulation | Open PRs to `develop` at sweep start: **0** (week-over-week: n/a, first record). | No |
| C4 | Review coverage | Commits landed on `develop` this week, enumerated from the branch, not the PR list: `e5e8450` only (via PR #3; SHA-bound verdict at `06b23e4`, tree-identical to the squash result `faab16b…` — verified). Zero direct pushes in the window. This pass's own merges (#6, #7, record PR) each carry SHA-bound verdicts. | No |
| C5 | Carve-out leakage | `carve-out-paths.js` run over every changed file this week (4 HTML pages): all `matched=false`. | No |
| C6 | Governance touch | Landed this week before this pass: **0**. This pass's own: PR #7 (manifest + CLAUDE.md) — accounted below under "autonomous changes", merged under the repair test with fresh-reader read. | Accounted |
| C7 | Check honesty | Both run summaries cite only `verify-affordances`, `ci` (verify+gates) and `carve-out-label` — all runnable and green. No null check cited. | No |
| C8 | Backlog starvation | Stage 3 skipped on **2 consecutive runs** (trigger is 3+), cause known and named: the model-attribution block (#5), not selection or a dry queue. Fix landed this week; R5 verifies. | No (on trajectory; intercepted) |
| C13 | Production staleness | `rev-list origin/main..origin/develop` = 0 at sweep (main was 1 *ahead* — see C14). After this pass's repairs develop leads by content-empty merge/governance commits awaiting normal future promotion. Weeks since last promotion: 0 (promoted 2026-08-28). | No |
| C14 | Production provenance | One promotion this week: PR #2, merged **by the owner personally** (2026-08-28T06:02Z) — authority self-recorded by GitHub. But: no ledger line existed, and **the back-merge was skipped** — `merge-base --is-ancestor` failed. | **Yes — repaired.** Ledger written retroactively on PR #2; ancestry restored via PR #6 (tree-identical, zero content change) in this session, per the condition's remedy |
| C16 | Panel honesty | 1 merged PR this week (#3): verdict SHA-bound at branch head `06b23e4`, squash-merged as `e5e8450`, **trees identical** (`faab16b…`) — not a stale-head verdict. Catch rate: 1 verdict / 0 diff changes — appropriate: the diff was mechanical transcription of the owner's own answers. Stated plainly: reviewer, author and merger are one account and one model family; this is visibility, not independence. | No |
| C17 | Delivery-process conformance | Three divergences found, classified: (1) manifest `model.current` vs §3 ladder — **drift against the repo, fixed** (PR #7); (2) `needs-senior-model` label absent — drift, **fixed** (label created); (3) no standing senior-review role — observed process is Routine-track only, review requirement met by the loop's internal review + Director verdicts; adequate for a static-HTML repo *today*, becomes drift the day a Hard-track issue appears with no senior pass to route to. Watching, not filing. | Fixed / fixed / watching |

**Check the worker exists:** it does — `loop-app-sites`, daily 05:43 local on this host,
2 runs on record, prompt renders and executes as written. It was *blocked*, which is C1's
blind spot (a blocked loop reports success). The **class**, not the instance: configuration
that lives outside the repo (the scheduler's model assignment) can contradict configuration
inside it (the manifest pin), and nothing checks the two against each other at install
time. The attribution gate catches it at *run* time — detection exists, prevention doesn't.
The instance is fixed by PR #7; the class is worth one line in the pack's adoption
checklist ("state which model the scheduler will supply; check it against the manifest"),
which this pass records here as pack-facing input rather than writing into another repo.

**Roster (hiring-manager view):** two roles — `loop-app-sites` (daily, sonnet-class) and
`director-weekly-app-sites` (weekly Monday, fable-class). No echo between them; the tier
split now matches the ratified ladder exactly. The loop's daily cadence against a 1-issue
backlog is generous but cheap while runs are no-ops; right-sizing trigger registered
(CONDITIONS.md R4) instead of acting early on one week of data. No hire needed: the queue
is not starved, it is gated on #5 (fixed) and one owner decision (#4). No role merged,
paused, or fired. Score next week against R4/R5.

**Register sweep:** `records/director/CONDITIONS.md` created this pass (did not exist).
Five rows seeded: R1 rendered-page/live-serving gap, R2 Apple-retention spot-check,
R3 legal review before scale, R4 loop cadence right-sizing, R5 model-fix verification —
each with trigger, remedy, and kill criteria. First sweep: R1 CHECKED-OK (risk stands,
noted), R2–R5 NOT-YET-DUE.

**Class of failure with no gate at all:** a page that parses clean but renders broken —
or a live site that stops serving — reaches the public with nothing in this system
noticing. `check-html` verifies structure; **no environment here or in CI renders a page**
(ENVIRONMENTS.md), and nothing monitors https://wickstacks.lifelift.app/ from outside.
Registered as R1 with an owner-facing option (external uptime/content check) rather than
silently accepted. Backup/restore: **N/A — no datastore is the product asset**; the asset
is the git repo, replicated GitHub-side and on this host's clone (an off-provider copy by
construction).

**Signal, not motion:** the week's motion was real signal — the site went from draft
markers to live, resolved pages (PR #3 → promotion #2 → verified serving with cert).
Current priority (#4) is dispatchable except for its owner-gated half, now split
explicitly on the issue. Backlog-vs-purpose (monthly): first month; README states the
purpose and the 1-issue backlog matches it. Done is defined for #4 (deep URLs preserved,
root decision executed).

## Transcript spot-check

Random pick (recorded): coin chose **run 1** (2026-08-28, summary 03:17Z). Its transcript
is **not locatable from this host** — the session store holds exactly one loop session,
which is run 2's. Reported by name, not skipped; substituted the reachable transcript:
**session `local_eb259734-d586-45b1-a431-af6a13e2a748`** (run 2, 2026-08-29 12:44Z),
read end-to-end (launch → render → model-gate discovery → Stage 0 diagnostics →
recurrence comment on #5 → log entry on #1 → summary → post-run human prompt "Raise any
issues with the Director" → durable escalation to pack #165 **with an empirical HTTP 404
probe of the dead hotline trigger**).

**Verdict: actions match the report.** Escalations were durable (#5 updated, #1 posted,
#165 commented) and the prior-escalation claim ("updated #5 rather than refiling")
resolves to a real comment. Two observations:

1. **Filed on run 1's behalf:** run 1 spotted an owner decision inside issue #4 (landing-
   page relocation) and parked it *only in its summary* — the exact "concerns that live
   only in the summary" class. Filed durably on #4 by this pass, citing the run.
2. Both summaries end addressed to the launcher ("**Needed from you:** decide…") — the
   "summary addressed to the owner" shape. Mitigated here because the same concern was
   also tracked in #5, and the human did read the session (the 18:06Z follow-up proves
   it). First formal instance; per the twice-in-a-row rule, no prompt fix filed yet —
   if next week's summaries do it again, the fix is owed at the pack's loop-monitor
   template, not here.

`loop-escalation` queue reconciled: one open escalation (#5) — correctly filed, correctly
updated instead of duplicated, now resolved pending R5 verification; no owner-blocker
aged past C2; no repeatedly-hit-but-never-escalated condition found in either run.

## Stage 2 — drift check

- Stage 0.4 output re-read: all declared commands execute; scan clean; the null tier is
  honestly null.
- Hook-pointer gate (`.claude/hooks`, directory named): **3 files clean** — 3 is the
  expected count (session-start, shared-clone guard, carve-out warn).

## Stage 3 — prompt-system findings

**Factual (shipped this week, PR #7):**
1. Manifest `model` block contradicted both measured reality (two sonnet runs) and the
   ratified ladder (§3 fleet default). Replaced: `current: claude-sonnet-5`, trailer to
   match, `accepted: ["claude-fable-5"]` (the Director demonstrably produces work here).
2. Manifest declared a dead escalation hotline (trigger deleted 2026-08-16 per owner note;
   live probe 404). Removed; capability `escalation-hotline: false` — the render-time flag
   cleanly drops the hotline sections from future rendered prompts. `check-capabilities`
   went from "1 advisory UNPROVEN" to clean.
3. CLAUDE.md described the custom domain as pending; Pages API says verified/approved/
   enforced. Replaced with the live fact (relative-links rule untouched).

**Red-team outcome on PR #7 (fresh reader, procedurally independent):** verdict
NEEDS CHANGE — the three repairs confirmed sound (every citation re-verified, schema
byte-identical to the pack's at v0.9.1, render clean at the pin, and the "keep fable as
current" alternative shown *worse*: review machinery re-validates only `current`, so it
would leave the model doing 100% of loop execution permanently unvalidated). Caught and
fixed before merge: a false clause this pass had written (the org default domain
**301-redirects** to the custom domain; it does not serve as an alternate path — probed),
two stale "DNS pending" assertions left behind (manifest `production_deploys_to`,
ENVIRONMENTS.md custom-domain row), a provenance error in the narrative (above), and the
then-missing label + authority comment. Under-stated risks now on the record:
`claude-fable-5` in `accepted` is a model whose behavioural claims nothing re-validates —
acceptable only because fable authors Director/governance work that is human-gated at
promotion; the pack rubric at v0.9.1 is validated against neither sonnet-5 nor fable-5,
and `check-rubric-validation`/`check-model-attribution` are unwired in this repo's CI —
attribution is prompt-enforced only. Cosmetic pre-existing: one lowercase
`Co-authored-by:` trailer in history (`e5e8450`) that a case-sensitive attribution sweep
would misreport as missing.

**Behavioural (all unmeasured — carried, not acted on):**
1. Loop summaries address decisions to the launcher rather than routing them exclusively
   to issues (see spot-check §2). Unmeasured; watch for recurrence.
2. No eval corpus exists in this repo (`eval-corpus: false`), so any future behavioural
   prompt claim here ships unmeasured by construction. Acceptable for a repo of this
   size; stated so the limitation is on the record, not discovered later.

## Owner actions (pre-staged; owner absent this pass)

1. **Issue #4 — the root-swap decision** (age 1 day; blocks the owner-gated half of the
   only backlog item). Question, made bounded on the issue itself: *swap the root to the
   Wickstacks page — yes/no; if yes, does the developer landing move to `/about/`, a
   separate site, or get dropped?* Recommendation: none — genuinely product taste.
2. **Model routing — audit/veto window** (age 0; nothing blocked). This pass moved the
   loop to sonnet-class per the ratified ladder (PR #7, full reasoning on #5). If the
   owner wants fable on this loop despite §3, one comment reverts it and the scheduler
   becomes the thing to fix. Silence = the ladder stands.
3. **Legal review before scale** (standing, from PR #3's accepted flag): the privacy
   page's regional-rights and breach wording is minimal generic text, owner-accepted
   pending real legal review. Re-presented quarterly (R3, next ~2026-11-23). Nothing
   blocked today; a scale/marketing push before review is the risk.
4. **Apple-retention spot-check** (standing, from PR #3's flag): when the first real
   Sign-in-with-Apple user exists, check the Firebase console user record against the
   privacy page's claim (Apple identifier + provided-or-relay email). Owner-only
   (console access). Registered as R2.

## Autonomous changes this pass (audit list)

| Change | Vehicle | Authority named |
|---|---|---|
| Back-merge `main`→`develop`, ancestry restored | PR #6, merged | C14 remedy; repair test; SHA-bound verdict at `c1d0d27` |
| Manifest model block + hotline retirement + CLAUDE.md domain fact | PR #7, merged after fresh-reader red-team | Repair test (stated in verdict); C6-accounted. The `carve-out-label` check required `needs-prod-review` on it (governance = high-risk surface) — applied, add-only; production scope, so the label marks the change for the owner's promotion gate without holding the `develop` merge |
| Retroactive promotion ledger | Comment on PR #2 | C14 record duty |
| `needs-senior-model` label created | repo labels | C17 conformance (spec §1) |
| Missed escalation filed on run 1's behalf | Comment on #4 | Spot-check duty |
| Director analysis + close-rule status | Comment on #5 | Stage 4 |
| This record + CONDITIONS.md | Record PR | Stage 4 |
| Commit identity configured (worktree-local only) | git config | housekeeping, no repo change |

The fresh-reader red-team on PR #7 was a subagent of this session: **procedurally, not
architecturally, independent** — same model family end to end. This record makes no
independence claim beyond that; the independent signal this week is mechanical (tree
hashes, HTTP probes, GitHub's own merge/deploy records), and where a finding rests only
on this pass's reading, it says so in place.

## Open items carried to next week

- **#5 verification (R5):** did the 2026-08-30 loop run clear Stage 1/3? If yes, close
  #5 citing the run. If it hit the block again, that is next week's headline. (Week 1.)
- **#4 owner decision** (owner action 1). (Week 1.)
- **C17 watch:** senior-pass routing if a Hard-track issue appears. (Week 1.)
- Behavioural observation on summary addressing — prompt fix owed at the pack on second
  occurrence. (Week 1.)
