# Director record — app-sites, 2026-W38

**Produced by:** Director weekly pass, model `claude-fable-5` (scheduled task
`director-weekly-app-sites`; `AskUserQuestion` present in this session's registry per
Stage 0.6; owner presence probable — this run was manually re-triggered by the owner).
**Scheduled for Monday 2026-09-14; ran Wednesday 2026-09-17.** The Monday firing was
suppressed entirely by the account weekly limit (no run entry exists at all — a new
evidence shape versus W37's 1-turn deaths); the catch-up firing at 09-17T07:59Z died on
the same limit before doing any work, and the owner re-triggered this session manually
after the 4am-PT reset. **Second consecutive week with this exact pattern**; remediated
this pass (see decisions).

---

## Catch-up digest (read this if you read nothing else)

**Safety verdict: the human gate is holding.** Branch protection on `main` re-probed
live this pass (required checks `verify`/`gates`/`carve-out-label`, `enforce_admins` on,
force pushes and deletions off). Zero commits and zero deploys on `main` since the W37
promotion; `main` is an ancestor of `develop`; every commit that landed on `develop`
this week maps to a PR with provenance (C4 below); the loop touched nothing outside its
authority and said so in its own logs.

**The week's story: the W37 mechanism chain completed one link further, then stalled on
the outage.** Issue #15 (nav-chrome drift, filed by the W37 pass from review findings)
was picked up by loop run 9 within a day, fixed across all four pages plus a sibling
instance the issue never named, reviewed twice SHA-bound at the true head, merged by
run 10 under the full gate, and held for promotion with the correct reasoning stated
(auto-close only fires on a `main` merge). Then it sat: the fix was promotion-ready
2026-09-12 and no promotion PR existed until this pass staged one 5 days later, because
the only actor who stages promotions — this Director — lost its Monday firing to the
limit. **W37's outage cost nothing; W38's outage measurably delayed production.**

**The week's failure: third fleet outage in three consecutive weeks, second from the
account weekly limit.** The loop has zero run entries for 09-14/15/16 (suppressed
firings — nothing host-side or repo-side, only the `list_task_runs` gap and the issue #1
count reveal it); every Monday governance pass fleet-wide was likewise suppressed. The
limit resets **Wednesdays 4am PT**; this Director fired **Mondays** — the deepest point
of budget depletion, and both W37 and W38 Monday firings died there. Measured limit-class
loss for this repo so far: 6 loop-days (09-07/08/09, 09-14/15/16) plus two Director
Mondays. The capacity question is the owner's (owner action 2, week 3); the free
scheduling mitigation was taken this pass.

**Decisions made by this pass** (Director authority, reversible, on the record):
`director-weekly-app-sites` moved Monday 09:22 → **Wednesday 09:22** (post-reset;
de jure now matches the de facto Wednesday runs of W37 and W38; restore string recorded
in the task description; scored by new register row R6); promotion PR staged at the
audited tip `050a814` (SHA-bound audit comment on the PR; merge reserved for owner
authority); register history lines; no roster hires or fires.

**THE ONE THING: authorize promotion of `050a814`** — it carries the nav-chrome fix
(#15/#19) to the live site. Staged, audited, checks green; asked in this session if the
owner is present.

---

## Stage 0 — grounding

- Own worktree confirmed (`exciting-borg-baeb2d`); no git verbs aimed at the shared
  clone.
- `verify-affordances`: **clean** — gate/syntax/cantfail/carve_out/install all resolve
  and exit 0; lint/test_unit/test_real/build honestly null; prompt scan clean. Stage 0.4
  gate passed; this week's verified claims stand on running checks.
- Log thread (#1) count: expected 7 daily loop runs since the W37 sweep (09-11..09-17);
  observed **4 completed** (runs 9–12: 09-11, 09-12, 09-13, 09-17 — each posting claim +
  summary, all complete and accurate). **Missing: 09-14, 09-15, 09-16** — verified from
  `list_task_runs`, not inferred: zero run entries exist for those dates (suppressed
  firings), then a failed start 09-17T07:59Z (limit message), then run 12 succeeded
  post-reset. Run 12 itself surfaced the gap in its Stage 0 note without being asked —
  an improvement over W37, where the dead days were invisible until this pass counted.
- Interactive question tool: **present**; owner: manually re-triggered this pass,
  treated as present for Stage 4. Posture: full pass, owner-gated items asked directly.
- Last week's open items: **#15 scored — the loop picked it up** (run 9 next-day, merged
  run 10; the file→implement→review→merge chain worked unattended); fleet capacity
  question unanswered (week 3, re-presented below with new evidence); C17 senior-pass
  watch (week 4, still no Hard-track issue); pack-facing notes carried; eval-corpus
  limitation standing.

## Stage 1 — condition sweep (every condition, measured; window 2026-09-11T00:30Z..2026-09-17)

Commits enumerated from the branch (`git log develop --since`), each attributed to a PR.
Three commits landed after the W37 session's own artifacts: `2e2e147` (back-merge,
PR #17), `e18d992` (W37 ledger addendum, PR #18) — both produced and ledgered by the W37
session itself — and `050a814` (PR #19, nav-chrome fix), the week's one substantive
change.

| # | Condition | Measured | Fired? |
|---|---|---|---|
| C1 | Loop liveness | 1 merge to `develop` in the window (#19, 09-12, run 10, full gate). No mergeable PR sat while a live loop existed — #19 waited one day under the loop's own no-self-fast-track rule (correct), and runs 11/12 held nothing. The 3 missed days are the fleet outage (headline), not a C1 blockage. | No |
| C2 | Staleness | Stalest actionable open item: issue #15, age 7 days — but advanced within 1 day of filing (fix merged 09-12); its only remaining mover is promotion, which is this pass's own staged action, executed today. Fleet capacity question: week 3, owner-gated, re-asked this session with its age. Nothing sat unaddressed that an agent could have moved — except the promotion staging itself, which waited 5 days on this Director's dead Monday firing; that is the outage finding, remediated by the schedule move. | No |
| C3 | Queue accumulation | Open PRs to `develop` at sweep: 0 (last week 0). No PR with zero completed checks. Promotion PR staged by this pass after the sweep. | No |
| C4 | Review coverage | All three commits map to PRs with provenance at their true heads: #19 two verdicts (haiku + sonnet tier) both `CONFIRMED_GOOD` SHA-bound at `5cc9d6f`, squash tree **byte-identical** to the reviewed head (`416360f`, verified this pass); #18 Director verdict SHA-bound at `d6622ff` (records-only ledger addendum, W37 Stage 4 duty); #17 back-merge — no separate verdict, provenance is promotion #10's SHA-bound authority + ledger, and its merge tree is **identical to both parents' trees** (`72fd72b` = `main`'s tree = the promoted pin's tree — zero content smuggled, verified this pass). Zero direct pushes. | No |
| C5 | Carve-out leakage | Classifier run over every file changed on `develop` this week (4 HTML pages, the records file): all `matched=false`. The two policy pages changed **chrome only** — diff read line-by-line by this pass: brand relabel, redundant link removal, `aria-current`; zero policy wording touched (content tripwire satisfied; reviewers verified the same). | No |
| C6 | Governance touch | Governance commits landed on `develop` in the window: **0**. This pass changed no repo governance file. Host-side (not a repo file, accounted here for the audit): `director-weekly-app-sites` schedule Monday→Wednesday — who: this Director; why: two consecutive Monday firings dead on the weekly limit, which resets Wednesdays 4am PT; test: cadence-tracking authority (frequency unchanged, cheap, reversible, restore string in the task description); scored by R6. | No |
| C7 | Check honesty | Run summaries cite `verify-affordances`, `npm run precommit`, the carve-out classifier, CI runs — all runnable per Stage 0.4. No null check cited. | No |
| C8 | Backlog starvation | No 3+ consecutive skips: run 9 shipped #19, run 10 merged it, runs 11/12 were honest no-ops that **independently re-ran** the empty-backlog fallback probes (CSS-selector liveness, comment sweep) rather than trusting the prior run's claim — the right behavior on a byte-identical tree. Queue honesty: after #15 closes on promotion, the backlog is empty (a roadmap fact, not a process failure; R4 watch below). | No |
| C13 | Production staleness | **Fired — new instance, week 1.** `rev-list main..develop` = 3 at sweep; substantive content: the #19 nav fix, promotion-ready since 09-12 (5 days); last promotion 09-11 (6 days). Mechanism: Director stages, owner authorizes — the stager's Monday firing died on the limit, so no promotion PR existed at sweep. **Remedy executed: promotion PR staged this pass at the audited tip** (record + addendum + nav fix cross together), SHA-bound audit comment posted, owner asked in-session. Root cause remediated by the schedule move. | Yes — remedied to the gate |
| C14 | Production provenance | Zero commits on `main` since the W37 promotion; the only deploy (`pages-build-deployment` at `f7551c0`, 09-11) maps to recorded promotion #10; `merge-base --is-ancestor` holds. | No |
| C16 | Panel honesty | 1 loop merge, 2 verdicts, both SHA-bound at the true head; tree identity verified mechanically. Catch rate this week: **0 of 1 initial review rounds changed the diff** — both reviewers confirmed a genuinely trivial chrome fix; probes were executed (grep output quoted, fresh clone checked out at the exact head), so these are verified confirmations, not rubber stamps, but the distinction rests on reading the probe output, which this pass did. Same-vendor caveat stands: one account, one model family — visibility, not independence. | No |
| C17 | Delivery-process conformance | Review mechanism VISIBLE / BINDING / SHA-BOUND on #19 as spec §5 requires. Routing: **2 reviewers (haiku + sonnet) on a Routine-track chrome fix — 3rd week running above the spec's 1-pass ceiling.** This week's data point cuts the other way from W37's: both reviewers confirmed, the second caught nothing — supporting the ceiling. Net evidence across three weeks: one correctness catch (#13), two no-gain second passes (#9-era style rounds, #19). Still filed **against the spec as a candidate amendment question** for the pack (the spec owns the call); the repo-side divergence remains unremediated drift only if the pack rules the ceiling stands. Carried (week 3, pack-facing): rendered C17 cites `docs/DELIVERY-PROCESS.md` as if repo-local; it exists only in the pack clone. | No |

**The worker exists.** Verified from output: 4 completed runs, 1 implementation + 1
gated merge, honest no-ops with re-executed probes on the quiet days, and the outage
gap self-reported by the loop's own Stage 0 note before this pass counted it.

**Roster (hiring-manager view):** two roles, unchanged; no hire, no fire; one schedule
move. `loop-app-sites`: 4/4 completed runs accurate (spot-check below); run 9 was the
week's high-yield run — next-day pickup of #15, sibling-instance sweep beyond the
issue's text, no self-fast-track. Run quality remains not the problem; run survival is —
and for the *daily* loop no schedule dodges a 3-day limit window, so its exposure is
owner question 2. `director-weekly-app-sites`: Monday firing mortality now 2/2 —
**remediated by moving the task to Wednesday post-reset** (the day this pass has
actually run for two consecutive weeks); R6 scores it next week. Cadences otherwise fit
the queue.

**Register sweep** (`records/director/CONDITIONS.md`, history lines updated this pass):
- **R1 CHECKED-OK** — still no external monitor; a **fourth** unwatched window
  (09-14→09-17). Live probes this pass: `/`, `/wickstacks/privacy/`,
  `/wickstacks/delete-account/` all HTTP 200; root serves the promoted Wickstacks page;
  the #19 nav fix is **not yet live** (zero `aria-current` on the served root —
  expected until this week's promotion merges, and itself a clean mechanical marker for
  the post-promotion acceptance probe).
- **R2 NOT-YET-DUE** — no known Apple-provider user.
- **R3 NOT-YET-DUE** — next re-present ~W48; no scale activity.
- **R4 NOT-YET-DUE — but approaching.** Runs 11/12 were honest no-ops on a non-empty
  queue (#15 held for promotion, correctly). Once #15 closes, the queue is empty; three
  consecutive no-ops on an empty queue trip the row, plausibly by W39. The remedy is
  pre-agreed (halve cadence, Director authority) — next week's pass should expect to
  execute it rather than re-deliberate.
- **R6 registered this pass** — Director schedule-move verification, self-retiring:
  TRIGGERED→retire when the 2026-09-24 Wednesday firing completes a full pass without
  manual re-trigger (verify from `list_task_runs` + the W39 record's own existence);
  escalate to owner question if it dies on the limit again (would mean the reset-day
  assumption is wrong or the budget exhausts within a day of reset).
- New registrations beyond R6: none — a repo-local limit-continuity row would duplicate
  Stage 0.5's count, as concluded in W36 and W37.

**Class of failure with no gate:** newly measured member — **scheduler suppression
leaves zero trace anywhere**: no repo-side evidence, no host-side run entry, not even a
1-turn transcript (W37's limit deaths at least left those). Detection was this pass
reading a gap in `list_task_runs` against the calendar, 3 days late. Three weeks, three
blind-spot shapes (host-down, limit-death, limit-suppression), all converging on the
same structural fact: **the fleet has no liveness signal that lives outside the thing
being measured.** This repo's exposure is bounded (static site, owner-gated production)
and its Directors now dodge the worst window; the durable fix remains fleet-level and
is owner question 2. Backup/restore: N/A — no datastore; the repo is the asset,
replicated by construction.

**Signal, not motion:** the one substantive motion (nav fix) was real signal — a
user-visible defect on the promoted site, found by review, filed, fixed, merged, and
now staged for production. Backlog honesty: after promotion, zero loop-workable issues
remain; the queue needs either owner-sourced work or R4's cadence cut. Monthly
backlog-vs-purpose read: done W35, due ~W39 — next pass.

## Transcript spot-check

Random pick recorded: `$RANDOM % 4` over completed runs {9, 10, 11, 12} → **run 9**
(2026-09-11, session `local_e255fc2a-6ef8-429b-8236-4ba480a3107e`, worktree
`vibrant-bhaskara-a1569e`), read as actions. **Verdict: actions match the report.** The
run really read all four pages before editing, really ran `npm run precommit` and the
carve-out classifier (output in-transcript, not asserted), honestly flagged the
redirect-stub fix as a sibling instance the issue never named, opened #19, commented on
#15, declined to fast-track its own PR under the prompt's rule, and posted claim +
summary. No concern lives only in the summary; no text addressed to the owner; its one
prior-state claim (develop advanced only by owner-driven promotion traffic) resolves
against git history. **Nothing filed on its behalf.**

`loop-escalation` queue reconciled: empty and clean — no open items, no owner-blocker
beyond what Stage 4 asks this session, no repeatedly-hit-but-unescalated condition in
the summaries read. Run 12 correctly routed the limit gap to its Stage 0 note rather
than a fresh escalation (the fleet question already stands, week 3).

## Stage 2 — drift check

- Stage 0.4 output re-read: all declared commands execute; scan clean; null tier
  honestly null.
- Hook-pointer gate (`.claude/hooks`, directory named): **3 files clean** — 3 is the
  expected count.

## Stage 3 — prompt-system findings

**Factual:** none new in this repo's prompt surfaces — W37's CLAUDE.md repair holds;
grep sweep for stale root-swap references found none outside `records/`.

**Factual, pack-facing (inputs for the pack Director, not actionable here):**
(1) carried week 3 — rendered C17 names `docs/DELIVERY-PROCESS.md` without a repo
qualifier; (2) review-ceiling evidence updated — W37's counterexample (senior-tier
catch) now sits beside a W38 counter-point (second reviewer added nothing on #19); net:
one catch, two no-gain rounds; the spec owns the call and now has both directions on
record.

**Behavioural:** none new. Carried (week 4): no eval corpus exists in this repo, so any
behavioural prompt claim here ships unmeasured by construction — stated, accepted.

## Owner actions

1. **Authorize promotion of `050a814`** (nav-chrome fix + W37 addendum + this record) —
   staged this pass, audited, checks green; asked in this session. On sign-off the
   Director merges, watches the deploy, probes acceptance (`aria-current` present on the
   served root), back-merges, closes #15 with evidence, and ledgers — same protocol as
   W37.
2. **Fleet weekly-limit capacity** (week 3, third incident, sharpened): measured
   limit-class loss is now 6 loop-days for this repo across two incidents, plus every
   Monday governance pass fleet-wide, twice running. The free mitigation is taken
   (this repo's Director now fires post-reset); the daily loops cannot dodge a
   multi-day window by scheduling. Options unchanged: **accept** (repo-level cost
   remains low here; this week it delayed a production fix by 5 days but lost nothing),
   **reduce fleet-wide cadence** (cross-repo spend allocation — each repo's Director
   can execute its share on your ruling), or **raise the plan/limit** (money — yours
   alone). Recommendation for this repo alone: accept, re-scored after R6 verifies the
   schedule move. Asked in this session after the promotion question.
3. Standing, unchanged: **R3** legal review (~W48); **R2** Apple-retention spot-check
   when the first Sign-in-with-Apple user exists.

## Autonomous changes this pass (audit list)

| Change | Vehicle | Authority named |
|---|---|---|
| `director-weekly-app-sites` schedule Monday→Wednesday 09:22 (post-reset); restore string in task description | host scheduled-task config (not a repo file) | cadence-tracking authority (cheap, reversible, frequency unchanged); C6 accounting above; scored by R6 |
| R1–R4 history lines (W38) + R6 registration | this record PR | register sweep duty |
| This record | record PR → `develop` | Stage 4; Director verdict SHA-bound on the PR (same test as #16/#18: the pass's own mandated artifact, `records/**` only) |
| Promotion PR staged at `050a814` | PR base `main`, **not merged without authority** | C13 remedy; "stage the promotion — audit done, PR open, recommendation written — and ask" |
| Live-site probes (read-only curl) | — | R1 evidence gathering |

Independence statement: condition measurements rest on mechanical evidence where
possible (tree-hash identity for #19's squash and #17's back-merge, the carve-out
classifier's output, `list_task_runs` gaps, live HTTP probes, branch-protection API
reads, CI run records); where a finding rests only on this pass's reading (the C16
"verified confirmation" judgment, the C17 classification, the run-11/12 honesty calls),
it says so in place. Reviewer, author and merger remain one account and one model
family; this record claims visibility, not independence. New independent signal this
week: the `list_task_runs` gap evidence and the live-site `aria-current` probe are
artifacts outside the loop's own reporting.

## Open items carried to next week

- **Promotion of `050a814`** — staged this pass; if the owner was absent at Stage 4,
  this is W39's first question with its age.
- **Fleet capacity question** — week 3, three incidents measured. If unanswered at W40,
  re-present with the accumulated lost-day count (per W36's rule; count stands at 6
  loop-days + 2 Director Mondays for this repo).
- **R6** — schedule-move verification, expected to resolve 2026-09-24; W39 scores it.
- **R4 watch** — likely trips at W39 once the queue empties; remedy pre-agreed (halve
  loop cadence), execute without re-deliberation.
- **C17 senior-pass watch** — week 4; still no Hard-track issue.
- **Monthly backlog-vs-purpose read** — due ~W39 (next pass).
- **Pack-facing notes** — C17 path qualifier (week 3); review-ceiling evidence now
  two-sided (updated this week). Inputs for the pack Director; not actionable here.
- **Eval-corpus limitation** — standing.
- Closed this week: #15 pickup scored (the W37 file→loop-implement mechanism worked
  end-to-end); W37's Director-Monday mortality — remediated by the schedule move, R6
  verifies.
