# Director record — app-sites, 2026-W36

**Produced by:** Director weekly pass, model `claude-fable-5` (session launched by
scheduled task `director-weekly-app-sites`; owner not interactively present —
`AskUserQuestion` absent from this session's tool registry, probed per Stage 0.6).
**Scheduled for Monday 2026-08-31; ran Wednesday 2026-09-02.** The delay is part of this
week's headline: the task first fired 2026-09-02T10:24Z and was killed by the account
session limit; this record is from the post-reset resumption (~12:55Z).

---

## Catch-up digest (read this if you read nothing else)

**Safety verdict: the human gate is holding.** Branch protection on `main` re-probed live
this pass (required checks `verify`/`gates`/`carve-out-label`, `enforce_admins` on, force
pushes and deletions off). Zero commits and zero deploys on `main` this week; `main` is an
ancestor of `develop`; the loop stayed confined to `develop` and merged only under the
full gate.

**The week's headline: the whole fleet went silent for three days, and no repo noticed
until this pass counted summaries.** No sessions of any kind ran on this host between
~2026-08-30T07:00Z and 2026-09-02T07:30Z — not this repo's loop, not any other repo's
roles (host asleep/off or scheduler not firing; evidence: the host session store plus the
log-thread gap on #1). When the host came back, the missed tasks fired in a catch-up
burst that collided with the account session limit: this repo's 10:23Z loop firing died
on arrival with a single "session limit reached" message — zero work, zero log entry,
zero race risk (transcript read; session `local_d4475ad5`) — while the regular 12:43Z
firing then ran clean, caught everything up (merged PR #9 under the full gate), and
posted a complete, accurate summary. The system degraded correctly — nothing merged
unreviewed, nothing false was recorded — but detection of "the fleet is not running"
exists only as this weekly count, three days late at best. The durable fix is
fleet-level, not this repo's: owner question 3.

**The W35 model-routing fix is verified closed.** Loop run 3 (08-29) authored a real
commit and PR as `claude-sonnet-5` matching the manifest — no attribution block — and
closed #5 under the close rule stated on that issue; run 4 (09-02) re-confirmed the fix
holding. Register row R5 retired.

**Decisions made by this pass** (Director authority, reversible, on the record):
promotion to `main` staged as PR #10, pinned at the audited SHA with the audit in the PR
body — the merge is the owner's; stale first-run framing replaced in both scheduled-task
launch notes; `blocked-owner` label applied to #4; R5 retired; W35's behavioural watch
(summaries addressed to the launcher) closed — it did not recur.

**THE ONE THING:** answer the root-swap question on issue #4 (age 5 days): *does the
Wickstacks page take the site root, and if so where does the developer landing move —
`/about/`, a separate site, or dropped?* It gates the entire dispatchable backlog.
(Second, one click: merge promotion PR #10.)

---

## Stage 0 — grounding

- Own worktree confirmed (`vibrant-bhaskara-a1569e`); no git verbs aimed at the shared
  clone.
- `verify-affordances`: **clean** — gate/syntax/cantfail/carve_out/install all resolve
  and exit 0; lint/test_unit/test_real/build honestly null; prompt scan clean. Stage 0.4
  gate passed; the week's verified claims stand on running checks.
- Log thread (#1) count: expected 4 daily loop summaries since the last Director pass
  (08-30, 08-31, 09-01, 09-02); observed 2 completed runs — run 3 (executed 08-29
  evening, posted 08-30T02:20Z, so it covers the 08-30 expectation only if read
  generously) and run 4 (09-02). **08-31 and 09-01 have no run at all, and strictly the
  gap is three consecutive scheduled firings (08-30 12:43Z, 08-31, 09-01) that never
  launched a session.** This is the Stage 0.5 gap finding; cause attributed under the
  headline (host outage), verified from the host session store, not inferred.
- Interactive question tool: absent; owner not present. Posture: owner-absent pass —
  everything safe executed, owner items pre-staged.
- Pack adoption issue kaiserguy/agent-project-template#165: **closed 2026-09-02 by the
  pack's own Director** (noted independently by loop run 4). The W35 close-rule handoff
  worked; no cross-repo write was needed from this pass.

## Stage 1 — condition sweep (every condition, measured; window 2026-08-27..09-02, "since last sweep" = since 08-29 where stated)

Commits enumerated from the branch (`git log`), not the PR list; each attributed to a PR.

| # | Condition | Measured | Fired? |
|---|---|---|---|
| C1 | Loop liveness | 4 merges to `develop` in the 7-day window (#6/#7/#8 on 08-29 by the W35 pass; #9 on 09-02 by loop run 4). PR #9 sat mergeable 08-30–09-01 while **no loop existed to merge it** (host outage, headline) and was merged within ~8 minutes of the first run that did exist. Not a wedge: the class is worker-absence, not worker-blockage, and it is this week's headline rather than a C1 blockage. | No |
| C2 | Staleness | Stalest actionable open item: issue #4, age 5 days, owner-gated; put to the owner in W35, now labelled `blocked-owner` (this pass), re-put below with its age. Nothing ≥7 days. | No |
| C3 | Queue accumulation | Open PRs to `develop` at sweep: 0 (last week 0). #9 carried green completed checks its whole life — never zero-check. | No |
| C4 | Review coverage | Commits landed on `develop` since last sweep: `a50cf99` only — squash of PR #9 head `92902a3`, which carries **two SHA-bound `CONFIRMED_GOOD` verdict comments** (haiku-tier and sonnet-tier, probes executed) at that head; squash tree verified **identical** to the head tree (`c41d65d…`), so the verdicts bind the merged content. Zero direct pushes; every commit maps to a PR. | No |
| C5 | Carve-out leakage | `carve-out-paths.js` run over every file changed on `develop` this week: `assets/site.css` → `matched=false`. | No |
| C6 | Governance touch | Governance-file commits landed on `develop` this week: **0**. (This pass's record PR touches `records/**` only — classifier run on both files, `matched=false`.) | No |
| C7 | Check honesty | Run-3/run-4 summaries cite `verify-affordances`, `npm run precommit`, `check-html.js --all`, the carve-out classifier, and CI runs — all runnable per Stage 0.4. No null check cited. | No |
| C8 | Backlog starvation | No 3+ consecutive skips: run 3 shipped fallback work (PR #9); run 4 evaluated #4 (owner-gated — correctly not implemented), re-scanned for dead code, and declined to manufacture a second fallback. A considered no-op is not a skip. | No |
| C13 | Production staleness | **Fired — first time.** `rev-list main..develop` = 4 at sweep (`6abf55d` back-merge, `0fa948b` governance, `06e3e1a` records, `a50cf99` CSS prune). Oldest unpromoted commit 2026-08-29 (4 days); last promotion 2026-08-28 (<1 week). Mechanism: develop→main promotion PR + owner merge — not attempted since. First week is a note per the condition; **remedy executed: promotion staged as PR #10**, pinned at the audited SHA `a50cf99`, full audit in the PR body, `needs-prod-review` applied. The owner's action is one click. | Yes — staged |
| C14 | Production provenance | Zero commits on `main` this week; zero deploys (`pages build and deployment` last ran 08-28 at the promoted head); `merge-base --is-ancestor` holds. | No |
| C16 | Panel honesty | 1 merged PR (#9): two verdicts SHA-bound at `92902a3`, still the head at merge; trees verified identical. Catch rate 2 verdicts / 0 diff changes — proportionate for a −33 LOC mechanical diff whose probes were actually executed (fresh clone, greps, gates). **Cost note:** the spec requires exactly one qualifying review pass; two reviewers on a trivial diff is spend above spec — if cost ever needs trimming, trim reviewer count first (the condition's own guidance). Same-vendor caveat restated: one account, one model family — visibility, not independence. | No |
| C17 | Delivery-process conformance | PR #9 conforms to the Routine track exactly: execution tier sonnet-class (fleet default), deterministic gates before judgment (stated, not implied), qualifying visible adversarial review, autonomous Stage 2 merge with classifier run. No new divergence. Carried from W35: no standing senior pass — fires only when a Hard-track issue appears (none yet). Pack-facing note: the rendered condition cites `docs/DELIVERY-PROCESS.md` as if repo-local; the file exists only in the pack clone. | No |

**The worker exists** — and this week proves the inverse of the W32 lesson: the loop was
neither blocked nor broken; the **host** was off. A dead scheduler and a dead loop leave
identical repo-side evidence (a log gap); only the host session store distinguishes them.
That distinction is recorded here because next time it may have the other cause.

**Roster (hiring-manager view):** two roles, unchanged. `loop-app-sites`: 2 completed
runs this week, both high-yield (run 3 verified the model fix in practice and shipped a
−33 LOC cleanup; run 4 merged it under the full gate; both summaries complete and
accurate — spot-check below). `director-weekly-app-sites`: this pass. No echo between
roles, no noise; no hire, no fire, no cadence change (R4 not due — outage days are not
no-op runs). The W35 tier split (sonnet loop / fable Director) is now verified in
practice from both sides.

**Register sweep** (`records/director/CONDITIONS.md`):
- **R1 CHECKED-OK** — still no render/uptime monitor; risk stands. This week sharpened
  it: the live site kept serving through the outage precisely because GitHub Pages is
  independent of this host — which is why an *external* monitor is the right shape if
  the owner ever funds one, and why for three days nothing anywhere would have noticed a
  serving failure.
- **R2 NOT-YET-DUE** — no known Apple-provider user.
- **R3 NOT-YET-DUE** — next re-present ~W48; no scale activity observed.
- **R4 NOT-YET-DUE** — queue still 1 issue; the three missed days are host outage, not
  no-op runs, and do not count toward the trigger.
- **R5 RETIRED — verified.** Run 3 authored commit + PR as `claude-sonnet-5` with no
  attribution block and closed #5 under its close rule; run 4 re-confirmed on a second
  day. Result logged in the register's Retired section.
- New registrations: **none, considered and declined** — a repo-local "scheduled-run
  continuity" row would duplicate Stage 0.5's weekly count, and the real fix is
  fleet-level (owner question 3). Reasoning recorded so next week can disagree.

**Class of failure with no gate:** this week supplied a measured instance of W35's
abstract answer — for three days a serving failure, cert lapse, or defacement would have
been invisible to every layer, because the only watcher (this host) was off. R1 remains
the register's carrier. Backup/restore: N/A — no datastore; the repo is the asset,
replicated by construction.

**Signal, not motion:** the week's one motion (PR #9) was small but real signal —
verified dead-code removal. The queue is honestly gated: #4 is the whole dispatchable
backlog and its blocker is a 5-day-old owner decision, now machine-visible via
`blocked-owner`. Monthly backlog-vs-purpose read not due (done W35).

## Transcript spot-check

Random pick recorded: `$RANDOM % 2 = 0` → **run 3** of {run 3 (08-29/30), run 4 (09-02,
still in progress at pick time)}; session `local_0eb7cf5e-5475-4c12-91c8-f36dc47628b4`,
read end-to-end. **Verdict: actions match the report.** The run rendered the prompt at
the pin, verified the model fix in practice *before* claiming it (authored commit + PR
under matching attribution), executed real probes before removing CSS (grepped all four
pages; confirmed `.skip`/`nav` live and `.draft`/`.owner` dead), ran every gate it cited,
closed #5 citing itself per the close rule stated on the issue, declined to fast-track
its own PR, and posted claim + summary. Its one prior-escalation claim ("#4 already
durably flagged by the Director, so I didn't re-comment") resolves to the Director's
08-29 comment on #4. **Nothing filed on its behalf.**

Supplementary, outside the random protocol: the silent 09-02T10:23Z loop session
(`local_d4475ad5`) was also read — 1 turn, killed by the session limit before any
action. No unlogged work exists; the run-numbering on #1 ("fourth run") stays honest.

W35's behavioural watch — summaries ending with decisions addressed to the launcher
("Needed from you") — **did not recur** in run 3 or run 4; both end with "Commits
authored this run". Watch closed; no prompt fix owed at the pack.

`loop-escalation` queue reconciled: #5 closed by the loop with the verifying run cited —
the escalate→fix→verify→close mechanism worked end-to-end across three roles. No open
`loop-escalation` items; no owner-blocker aged past C2; no repeatedly-hit-but-never-
escalated condition in either transcript.

## Stage 2 — drift check

- Stage 0.4 output re-read: all declared commands execute; scan clean; null tier
  honestly null.
- Hook-pointer gate (`.claude/hooks`, directory named): **3 files clean** — 3 is the
  expected count.

## Stage 3 — prompt-system findings

**Factual, fixed this pass (host-side, quoted here for audit):** both scheduled-task
launch notes still carried first-run framing that run 4 had to discover was stale —
`loop-app-sites`: "this repo's roles never ran before … read the adoption-gap checklist
on pack issue #165 as Stage 0 input"; `director-weekly-app-sites`: "no Director record
has ever existed in this repo …". Replaced with steady-state notes pointing at #1 and
`records/director/` respectively, each stamped "updated by the W36 Director pass". These
are host files (`~/.claude/scheduled-tasks/*/SKILL.md`), not repo files; the edit is
roster maintenance, reversible, and recorded here because no diff exists in the repo.

**Factual, pack-facing (not actionable from this repo — the launch scope is "operate on
that repository only"):** (1) rendered C17 cites `docs/DELIVERY-PROCESS.md` with no repo
qualifier; the file exists only in the pack clone, so the condition as rendered names a
path that does not resolve here. (2) The adoption-checklist class from W35 has a second
member: nothing at install time checks scheduler-side context against repo reality —
W35 found the model assignment, W36 found launch-note staleness. Both are one-line
template fixes owned by the pack Director; recorded as input, not written there.

**Behavioural:** none new. Carried: no eval corpus exists in this repo, so any
behavioural prompt claim here ships unmeasured by construction (week 2; stated,
accepted).

## Owner actions (pre-staged; owner absent this pass)

1. **Issue #4 — the root-swap decision** (age 5 days; gates the entire dispatchable
   backlog; THE ONE THING). Bounded on the issue: *swap the root to the Wickstacks page —
   yes/no; if yes, does the developer landing move to `/about/`, a separate site, or get
   dropped?* Recommendation: none — product taste.
2. **Promotion PR #10** (new this pass) — one click. Audit in the PR body; pinned at
   `a50cf99`; recommendation: merge.
3. **Fleet scheduling SPOF** (new; fleet-level, so other repos' Directors likely saw the
   same event): every scheduled role across ~6 repos runs from one Windows host; this
   week that host was silent for three days and nothing alerted. Options: **accept**
   (status quo, zero cost — defensible while no role is time-critical) or **fund
   cloud-scheduled runs** for the roles that matter (recurring spend — a money decision,
   hence yours). Recommendation for this repo alone: accept; a static site with a
   1-issue backlog lost nothing. Decide fleet-wide where stakes are higher.
4. Standing, unchanged: **R3** legal review (~W48); **R2** Apple-retention spot-check
   when the first Sign-in-with-Apple user exists.

## Autonomous changes this pass (audit list)

| Change | Vehicle | Authority named |
|---|---|---|
| Promotion staged, pinned at audited SHA | PR #10 (base `main`), **not merged** | C13 remedy; merge reserved to the owner by branch protection + CLAUDE.md |
| `blocked-owner` label on #4 | repo labels | queue hygiene; apply-label authority; makes the C2 gate machine-visible |
| Stale first-run notes replaced in both task files | host `~/.claude/scheduled-tasks/{loop,director-weekly}-app-sites/SKILL.md` | Director roster management (factual repair; quoted above) |
| R5 retired + W36 history lines on R1–R4 | this record PR | register sweep duty |
| This record | record PR | Stage 4 |
| Worktree-local git identity | `git config` | housekeeping, no repo change |

Independence statement: the spot-check verdicts and condition measurements above rest on
mechanical evidence where possible (tree hashes, session stores, GitHub run/merge
records, executed gates); where a finding rests only on this pass's reading (e.g. the
"considered no-op" judgment on run 4's Stage 3), it says so in place. Reviewer, author
and merger remain one account and one model family; this record claims visibility, not
independence.

## Open items carried to next week

- **#4 owner decision** — week 2, age 5 days, now labelled `blocked-owner`. (Owner
  action 1.)
- **Promotion PR #10 awaiting owner merge** — new; if unmerged next week, C13 counts it
  with severity growing. After merge, the back-merge + ledger line is next session's
  first routine duty.
- **C17 senior-pass watch** — week 2; still no Hard-track issue.
- **Fleet SPOF owner question** — week 1. If unanswered in 4 weeks, re-present with the
  accumulated outage count.
- **Eval-corpus limitation** — standing.
- Closed this week: R5 (verified), #5 (closed by the loop), W35 behavioural watch (no
  recurrence).
